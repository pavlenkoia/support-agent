services:
  app:
    build:
      context: .
      dockerfile: deploy/compose/app.Dockerfile
    env_file:
      - deploy/env/app.env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - ./kb:/app/kb:ro
      - ./logs:/app/logs
    command: ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

  worker:
    build:
      context: .
      dockerfile: deploy/compose/worker.Dockerfile
    env_file:
      - deploy/env/app.env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./kb:/app/kb:ro
      - ./logs:/app/logs
    command: ["uv", "run", "python", "-m", "app.workers.main"]

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: support_agent
      POSTGRES_USER: support_agent
      POSTGRES_PASSWORD: support_agent
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U support_agent -d support_agent"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    name: ${COMPOSE_PROJECT_NAME:-support-agent}_postgres_data
