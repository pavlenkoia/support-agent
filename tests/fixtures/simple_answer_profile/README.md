# Simple Answer Engine fixture profile

This synthetic profile exists only for deterministic contract tests of `SimpleAnswerEngine`.
It contains a minimal prompt and two compiled KB pages. It is not customer KB and must never be mounted as a production profile.
